{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://research.opensourcemed.info/schema/ext/ebola.schema.json",
  "title": "PAIS extension namespace: ebola",
  "description": "Idiosyncratic Ebola-survivor fields that no other cohort reports (e.g. ocular sequelae). Layer-3 extension: validated only because this schema exists; namespaces without a schema are preserved and flagged 'unvalidated', never rejected. A namespace used by 3+ cohorts is a candidate for promotion into the Measure registry.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "baseline_ocular_exam_n": { "type": "integer", "minimum": 0 },
    "uveitis_laterality": { "enum": ["unilateral", "bilateral", "mixed", "not_reported"] },
    "n_eyes": { "type": "integer", "minimum": 0 }
  }
}
