# Optional PAIS instrument crosswalk

Version: `0.1.0`

This file is a set of assumptions about instrument equivalence, not source data.
Applying it produces estimates that are not in any paper: they are model output, not evidence.
Any downstream publication must say that it applied this crosswalk and cite its version. The
core estimate build never reads this directory.
